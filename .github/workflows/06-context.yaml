name: 06 - Contexts

on: push
    
jobs:
    echo-data:
        runs-on: Ubuntu-latest
        steps:|
            - name: Display Information
              run: |
               echo "Event Name ${{github.event_name }}"